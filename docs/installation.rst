============
Installation
============

At the command line::

    $ easy_install experiment

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv experiment
    $ pip install experiment
